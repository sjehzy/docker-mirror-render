services:
  - type: web
    name: docker-mirror
    env: docker
    # 正确的docker-compose文件配置字段（下划线分隔）
    docker_compose_file: docker-compose.yml
    # 免费实例计划
    plan: free
    # 仓库更新时自动部署
    autoDeploy: true